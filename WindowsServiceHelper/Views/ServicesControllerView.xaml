<UserControl x:Class="ServiceProcess.Helpers.Views.ServicesControllerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="300">
    <DockPanel>
        
        <ToolBar DockPanel.Dock="Top">
            <Button Command="{Binding StartCommand}">Start</Button>
            <Button Command="{Binding StopCommand}">Stop</Button>
            <Button Command="{Binding PauseCommand}">Pause</Button>
        </ToolBar>

        <ListView ItemsSource="{Binding Services}"
                  SelectedItem="{Binding SelectedService}" />
    </DockPanel>
</UserControl>
